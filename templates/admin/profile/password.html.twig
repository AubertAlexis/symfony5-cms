{% extends "admin/base.html.twig" %}

{% block title "profileResetPassword.title.title"|trans %}

{% block title_h1 "profileResetPassword.title.h1"|trans %}

{% block body %}
    <div class="card shadow mt-3">
        <div class="card-header d-flex align-items-center">
            <h6 class="m-0 font-weight-bold text-primary">{{ "profileResetPassword.title.form"|trans }}</h5>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                
                {{ block("password", "admin/profile/_form.html.twig") }}

                {{ block("submit", "admin/profile/_form.html.twig") }}

            {{ form_end(form, {"render_rest": false}) }}
        </div>
    </div>

    
{% endblock %}
